# Backend API URL (no trailing slash)
VITE_API_URL=http://localhost:3000

# Payment provider public key + sandbox URL (never put private keys here)
VITE_WOMPI_PUBLIC_KEY=pub_test_xxx
VITE_WOMPI_SANDBOX_URL=https://sandbox.wompi.co/v1
